# 当前状态

## 文件职责

本文件记录当前执行状态和最近的工作区观察。稳定产品含义与契约仍由 `README.md`、`docs/` 和 `docs/specs/` 负责。

## 当前判断

- 本仓库是一个 Windows-first 的 Python 3.11 + PySide6 桌面背单词应用。
- CodeGraph 已为 Python 源文件初始化，结构性代码导航应优先使用它。
- 本轮约束建立前，工作区中没有已跟踪的根目录 `AGENTS.md`。
- 仓库已包含 `docs/superpowers/` 下带日期的弹窗控制 plan/spec 文件。
- 不要把未勾选的 dated plan 步骤当作当前实现状态证明。实现状态必须通过源码、测试、CodeGraph 和运行时验证确认。
- 2026-06-08 观察到：在加入约束文件前，工作区已经存在许多未提交的源码/测试/build/docs 变更。除非有证据证明，否则都视为用户工作。

## 最近验证

- 2026-06-08 约束建立：新 guardrail 文件的读序/占位自查未发现未完成占位标记。
- 2026-06-08 约束建立：`py -3.11 -m pytest tests -q` 通过，结果为 46 个测试通过。
- 2026-06-08 文档中文化：仓库 Markdown 文档已改为中文说明，旧英文标题扫描无命中。
- 2026-06-08 文档中文化：`py -3.11 -m pytest tests -q` 通过，结果为 46 个测试通过。
- 2026-06-08 FSRS + SQLite 实现核验：`py -3.11 -m pytest tests -q` 通过，结果为 66 个测试通过。
- 2026-06-09 打包/安装器实现核验：`py -3.11 -m pytest tests -q` 通过，结果为 68 个测试通过。
- 2026-06-09 打包/安装器实现核验：`.\build\build_exe.ps1` 成功生成 `dist/oh-my-word-py/oh-my-word-py.exe`；`.\build\build_installer.ps1 -SkipPortableBuild` 成功生成 `dist/oh-my-word-setup.exe`。
- 2026-06-09 打包/安装器实现核验：安装器 payload 未包含 `storage/`，包含 `_internal/data/wordbooks/` 词库；未执行真实安装交互或卸载交互。
- 2026-06-09 例句展示与朗读文本修复：`py -3.11 -m pytest tests -q` 通过，结果为 73 个测试通过；未执行 Windows 桌面视觉检查或真实 TTS 听音检查。
- 2026-06-09 默认词库整理：`data/wordbooks/` 默认只保留 `zz_kaoyan_enriched.json` 和 `SOURCES.md`；增强词库覆盖 5528 条，缺音标 0，空/占位例句 0；`py -3.11 -m pytest tests -q` 通过，结果为 74 个测试通过。
- 2026-06-11 TTS 初始化改为主线程延迟 warm-up + 状态提示：`py -3.11 -m pytest tests -q` 通过，结果为 105 个测试通过；尚未执行 Windows 桌面运行时检查。
- 2026-06-13 VoxCPM 应用内管理：已新增设置页“发音”分类中的 VoxCPM 后台安装、启动、停止、检测和日志入口；已新增 `app/voxcpm_service.py` 管理独立安装目录、模型目录和本地服务进程；已新增 `voxcpm_install_root`、`voxcpm_model_cache_root`、`voxcpm_use_model_mirror`、`voxcpm_auto_start` 设置字段。
- 2026-06-13 VoxCPM 应用内管理验证：`py -3.11 -m pytest tests -q` 通过，结果为 127 个测试通过；`.\build\build_installer.ps1` 成功生成 `dist/oh-my-word-setup.exe`；portable payload 包含 `_internal/tools/voxcpm_service/`；打包后的 `dist\oh-my-word-py\oh-my-word-py.exe` 可启动并打开 `oh my word 设置` 窗口；本机 VoxCPM service 从 `%LOCALAPPDATA%\OhMyWord\voxcpm\start_service.ps1` 启动后 `/health` 正常，并成功生成 WAV。
- 2026-06-13 朗读内容模式与 VoxCPM 播放加固：新增 `pronunciation_content_mode`，设置页可选择只读单词、只读例句或单词加例句；默认 `word_and_example` 使用 `word.\n\nexample_sentence` 作为明确停顿，减少 VoxCPM 吞掉开头短词的概率；VoxCPM WAV 缓存改为 4 个固定文件轮换并在播放前停止旧音频。
- 2026-06-13 朗读内容模式验证：`py -3.11 -m pytest tests -q` 通过，结果为 135 个测试通过；PySide 设置窗口运行时检查确认“朗读内容”选项为“只读单词 / 只读例句 / 单词 + 例句”且可读回 `example`；`.\build\build_installer.ps1` 成功生成 `dist\oh-my-word-setup.exe`；打包后的 `dist\oh-my-word-py\oh-my-word-py.exe` 可启动，短启动检查后已关闭进程。未执行真实安装器点击安装/卸载，也未执行人工听音判断。
- 2026-06-13 VoxCPM 流式发音：service 新增 `POST /synthesize_stream`，使用 VoxCPM `generate_streaming()` 输出 `s16le` PCM chunk；桌面端 `VoxCpmHttpProvider` 默认优先走流式 endpoint，并通过 `StreamingPcmPlayer`/`QAudioSink` 边接收边播放，旧服务 404/405 时回退完整 WAV。
- 2026-06-13 VoxCPM 流式验证：`py -3.11 -m pytest tests/test_tts.py tests/test_voxcpm_service_files.py -q` 通过，结果为 35 个测试通过；`py -3.11 -m pytest tests -q` 通过，结果为 137 个测试通过；FastAPI TestClient fake model 验证 `/synthesize_stream` 返回 `48000/s16le` PCM；真实 VoxCPM 本地模型完整消费 `generate_streaming()` 得到 5 个 chunk，总音频约 0.8 秒，日志显示 `cuda/bfloat16`；真实 endpoint TestClient 返回 200 和 `s16le` PCM；`StreamingPcmPlayer` 用静音 PCM 初始化 `QAudioSink` 成功；`.\build\build_installer.ps1` 成功生成 `dist\oh-my-word-setup.exe`；打包后的 `dist\oh-my-word-py\oh-my-word-py.exe` 可启动，短启动检查后已关闭进程；portable payload 内的 `tools\voxcpm_service` 包含 `synthesize_stream`、`generate_streaming` 和 `s16le`。未做人工听音或真实安装器点击安装/卸载。
- 2026-06-13 选词加权随机：SQLite 主路径 `StudyStore.select_next_word()` 保持到期复习词、新词、未掌握旧词的池级优先级，但同一候选池内改为按待学习力加权随机，不再固定取字母序第一个词；待学习力权重考虑 overdue、difficulty、stability、lapses、unknown_count 和 show_count。
- 2026-06-13 选词加权随机验证：先新增失败测试 `tests/test_study_store.py::test_selects_due_card_by_learning_need_weight`，确认旧实现不接受 `rng` 且固定取第一个候选；实现后该测试通过，`py -3.11 -m pytest tests/test_study_store.py tests/test_words.py -q` 通过，结果为 25 个测试通过；`py -3.11 -m pytest tests -q` 通过，结果为 138 个测试通过。
- 2026-06-13 安装包刷新：在当前工作树上运行 `py -3.11 -m pytest tests -q` 通过，结果为 138 个测试通过；`.\build\build_installer.ps1` 成功刷新 `dist\oh-my-word-setup.exe`，文件大小 58,178,560 字节，修改时间 2026-06-13 19:36:52；构建后、短启动前检查 portable payload 包含 `_internal\tools\voxcpm_service\install_local.ps1` 和 `_internal\data\wordbooks\zz_kaoyan_enriched.json`，且未预打包 `dist\oh-my-word-py\storage`；打包后的 `dist\oh-my-word-py\oh-my-word-py.exe` 可启动，短启动 5 秒后已强制关闭，并按运行契约生成 `dist\oh-my-word-py\storage\app.log` 与 `oh_my_word.sqlite3`。未执行真实安装器点击安装/卸载。
- 2026-06-13 VoxCPM 当前服务检查：本机 `127.0.0.1:8808` 的 `/health` 返回 200，`/synthesize` 返回 200，但 `/synthesize_stream` 返回 404；端口进程命令行为 `python.exe -m uvicorn service.server:app --host 127.0.0.1 --port 8808`，说明当前正在运行的 companion service 仍不是支持流式 endpoint 的新版服务。重新安装主程序不会自动替换已存在并正在运行的 VoxCPM service；需要停止旧服务并通过设置页“后台安装 / 更新”或安装器 VoxCPM 选项刷新 service。
- 2026-06-13 v0.1.1 版本化与安装包：新增 `app/version.py` 作为单一版本源，当前版本 `0.1.1`；设置页新增“关于”分类显示当前版本和更新日志；安装器默认输出 `dist\oh-my-word-setup-v0.1.1.exe`，安装器窗口标题/主标题显示 v0.1.1。
- 2026-06-13 v0.1.1 打包边界：`build_exe.ps1` 和 `build_installer.ps1` 只打包 VoxCPM service-only allowlist：`install_local.ps1`、`server.py`、`engine.py`、`requirements.txt`、`README.md`；不再整目录打包 `tools/voxcpm_service/`，避免误带 `.venv`、模型、Torch/CUDA wheel、Hugging Face/ModelScope 缓存或 `__pycache__`。`install_local.ps1` 更新时会清空并刷新已安装的 `service/` 目录，并校验复制后的 `server.py` 包含 `synthesize_stream`。
- 2026-06-13 v0.1.1 验证：`py -3.11 -m pytest tests -q` 通过，结果为 143 个测试通过；`.\build\build_installer.ps1` 成功生成 `dist\oh-my-word-setup-v0.1.1.exe`，文件大小 58,173,952 字节，修改时间 2026-06-13 22:42:58；portable payload 中 `_internal\tools\voxcpm_service\` 仅包含 5 个 allowlist 文件，未包含 `__pycache__` 或 `.venv`，构建后短启动前未预打包 `storage`；打包后的 `dist\oh-my-word-py\oh-my-word-py.exe` 可启动，短启动 5 秒后已强制关闭。未执行真实安装器点击安装/卸载，也未执行 VoxCPM service 真实后台更新。
- 2026-06-13 v0.1.2 VoxCPM 首尾漏音处理：按 VoxCPM 官方 API 参数显式启用 `retry_badcase=True`、`retry_badcase_max_times=3`，默认 badcase ratio threshold 为 `4.0`；默认 `cfg_value` 降为 `1.5` 并允许通过环境变量微调；完整 WAV 和流式 PCM 都加入短首尾静音垫，降低短词起音/尾音被本机播放截断的概率。
- 2026-06-13 v0.1.2 验证：先新增失败测试确认 service 缺少 badcase 参数和首尾静音保护；实现后 `py -3.11 -m pytest tests\test_voxcpm_service_files.py tests\test_tts.py tests\test_version.py tests\test_settings_window.py -q` 通过，结果为 44 个测试通过；`py -3.11 -m pytest tests -q` 通过，结果为 144 个测试通过；`.\build\build_installer.ps1` 成功生成 `dist\oh-my-word-setup-v0.1.2.exe`，文件大小 58,176,000 字节，修改时间 2026-06-13 23:07:37；portable payload 中 `_internal\tools\voxcpm_service\` 仅包含 5 个 allowlist 文件，未预打包 `storage`；短启动 `dist\oh-my-word-py\oh-my-word-py.exe` 5 秒成功并生成 exe 同级 `storage\app.log` 和 `oh_my_word.sqlite3`。
- 2026-06-13 v0.1.2 本机 service 状态：构建后再次检查本机 `127.0.0.1:8808`，`/health` 返回 200，但 `/synthesize_stream` 仍返回 404，说明当前正在运行的 companion service 仍未被本轮打包自动替换。需要通过设置页“后台安装 / 更新”或运行新的 service-only 安装脚本刷新已安装 service 后，才能验证 v0.1.2 的流式和参数改动在本机实际生效。
- 2026-06-13 v0.1.2 GitHub 发布状态：`main` 已推送到 commit `535ab43 release: v0.1.2`，远端 tag `v0.1.2` 已推送。当前机器没有 `gh` CLI，也没有 `GITHUB_TOKEN/GH_TOKEN`，因此尚未自动创建 GitHub Release 页面，也尚未上传 `dist\oh-my-word-setup-v0.1.2.exe` 作为 release asset。
- 2026-06-14 v0.1.3 VoxCPM 停止/检测修复：根因是设置页“停止服务”只停止当前应用实例跟踪到的 `_service_process`；如果 8808 由上一轮应用或安装脚本启动的旧 `python.exe -m uvicorn service.server:app` 占用，停止动作不会结束该进程，随后“检测服务”仍会命中 `/health` 并把 UI 状态改回运行中。`app/voxcpm_service.py` 已改为停止当前跟踪进程，并额外识别同一 local endpoint 上命令行包含 `uvicorn` 和 `service.server:app` 的 VoxCPM 旧服务进程；同端口无关进程不会被终止。
- 2026-06-14 v0.1.3 验证：`py -3.11 -m pytest tests\test_voxcpm_service_manager.py -q` 通过，结果为 8 个测试通过；`py -3.11 -m pytest tests\test_controller.py tests\test_settings_window.py tests\test_voxcpm_service_manager.py -q` 通过，结果为 36 个测试通过；`py -3.11 -m pytest tests -q` 通过，结果为 147 个测试通过；使用新 manager 对当时占用 8808 的真实旧服务执行停止检查，停止前 `/health=True`，`stop_service()` 返回 `True`，停止后 `/health=False`。
- 2026-06-14 v0.1.3 安装包：`.\build\build_installer.ps1` 成功生成 `dist\oh-my-word-setup-v0.1.3.exe`，文件大小 58,180,096 字节，修改时间 2026-06-13 23:57:38；portable payload 中 `_internal\tools\voxcpm_service\` 仅包含 `install_local.ps1`、`server.py`、`engine.py`、`requirements.txt`、`README.md`，扫描未发现 `.venv`、模型权重、Torch/CUDA wheel、Hugging Face/ModelScope 缓存、`__pycache__` 或其它禁止 payload；短启动 `dist\oh-my-word-py\oh-my-word-py.exe` 5 秒成功并生成 exe 同级 `storage\app.log` 和 `oh_my_word.sqlite3`。
- 2026-06-14 v0.1.3 未覆盖项：未执行真实安装器点击安装/卸载；未执行真实设置页按钮点击验证；未执行人工听音。GitHub Release 创建/安装包上传仍取决于本机 `gh` 登录状态和网络权限。
- 2026-06-14 VoxCPM 语气提示词与短词边界：新增 `voxcpm_voice_prompt` 设置字段和设置页“语气提示词”输入框，默认空字符串；使用 `voxcpm_local` 时，controller 会按 VoxCPM Voice Design 格式在合成文本前添加 `(prompt)`，并将独立单词包装为 `"word".` 以强化短词边界。`system_qt` 路径继续使用普通朗读文本。该变更尚未做真实 VoxCPM 听音验证。
- 2026-06-14 v0.1.4 安装包测试版：版本号升至 `0.1.4`，更新日志记录 VoxCPM 语气提示词和短词引号保护；`py -3.11 -m pytest tests -q` 通过，结果为 150 个测试通过；`.\build\build_installer.ps1` 成功生成 `dist\oh-my-word-setup-v0.1.4.exe`，文件大小 58,182,656 字节，修改时间 2026-06-14 00:40:28；portable payload 中 `_internal\tools\voxcpm_service\` 仅包含 `install_local.ps1`、`server.py`、`engine.py`、`requirements.txt`、`README.md`，扫描未发现 `.venv`、模型权重、Torch/CUDA wheel、Hugging Face/ModelScope 缓存、`__pycache__` 或其它禁止 payload；短启动 `dist\oh-my-word-py\oh-my-word-py.exe` 5 秒成功并生成 exe 同级 `storage\app.log` 和 `oh_my_word.sqlite3`。按用户要求，本轮先不推送 GitHub，等待用户测试后再提交、推送、打 tag 和创建 Release。
- 2026-06-14 VoxCPM 停顿/句中卡顿调整：针对用户反馈“停顿感很强、句中一卡一卡”，VoxCPM 专用 `word_and_example` 文本从 `"word".\n\nexample` 改为 `"word". example`，保留短词引号保护但去掉双换行强停顿；`StreamingPcmPlayer` 新增约 0.35 秒 PCM 预缓冲，在启动 `QAudioSink` 前先攒一小段音频，降低模型 chunk 生成略慢导致的句中 underflow 卡顿。`py -3.11 -m pytest tests -q` 通过，结果为 151 个测试通过；已重新构建 `dist\oh-my-word-setup-v0.1.4.exe`，文件大小 58,182,144 字节，修改时间 2026-06-14 00:49:48；payload 边界检查未发现禁止文件，短启动 5 秒成功并生成 exe 同级 `storage\app.log` 和 `oh_my_word.sqlite3`。尚未做真实 VoxCPM 听音验证，且按用户要求仍未推送 GitHub。
- 2026-06-14 v0.1.5 VoxCPM 可调流式预缓冲：设置页“发音”分类新增 `流式预缓冲`，对应 `voxcpm_stream_prebuffer_seconds`，默认 `0.35` 秒，范围 `0.00` 到 `2.00` 秒；保存设置后 controller 会在该值变化时重建 TTS 后端，使后续 VoxCPM 流式朗读按新预缓冲时间启动。该版本同时包含 v0.1.4 的语气提示词、短词引号保护，以及去掉 `word_and_example` 双换行强停顿的听感调整。
- 2026-06-14 v0.1.5 验证：`py -3.11 -m pytest tests\test_settings.py tests\test_settings_window.py tests\test_tts.py tests\test_controller.py tests\test_version.py -q` 通过，结果为 61 个测试通过；`py -3.11 -m pytest tests -q` 通过，结果为 154 个测试通过；`git diff --check` 通过，仅有 Windows 行尾提示；`.\build\build_installer.ps1` 成功生成 `dist\oh-my-word-setup-v0.1.5.exe`，文件大小 58,181,632 字节，修改时间 2026-06-14 01:04:55；portable payload 中 `_internal\tools\voxcpm_service\` 仅包含 `install_local.ps1`、`server.py`、`engine.py`、`requirements.txt`、`README.md`，扫描未发现 `.venv`、模型权重、Torch/CUDA wheel、Hugging Face/ModelScope 缓存、`__pycache__` 或其它禁止 payload；短启动前未预打包 `storage`，短启动 `dist\oh-my-word-py\oh-my-word-py.exe` 5 秒成功并在 exe 同级生成 `storage\app.log` 和 `oh_my_word.sqlite3`。未执行真实安装器点击安装/卸载，也未执行真实 VoxCPM 人工听音。
- 2026-06-14 v0.1.6 VoxCPM TTS 目录整理：默认 VoxCPM 安装目录改为当前软件运行目录下的 `tts\voxcpm`，模型目录改为 `tts\voxcpm\models`；安装器中主程序安装目录变化时，会同步更新未手动编辑过的 VoxCPM engine 和模型目录；旧版默认路径和静态 fallback 会在应用运行时迁移到当前运行目录下的 `tts\voxcpm`，用户手动指定的自定义路径不会被覆盖。
- 2026-06-14 v0.1.6 验证：先新增失败测试覆盖 `tts\voxcpm` 默认布局、controller 运行时默认迁移、安装器默认路径跟随主安装目录；实现后 `py -3.11 -m pytest tests\test_settings.py tests\test_settings_window.py tests\test_controller.py tests\test_voxcpm_service_files.py tests\test_version.py -q` 通过，结果为 81 个测试通过；`py -3.11 -m pytest tests -q` 通过，结果为 158 个测试通过；`git diff --check` 通过，仅有 Windows 行尾提示；`.\build\build_installer.ps1` 成功生成 `dist\oh-my-word-setup-v0.1.6.exe`，文件大小 58,185,216 字节，修改时间 2026-06-14 01:31:43；portable payload 中 `_internal\tools\voxcpm_service\` 仅包含 `install_local.ps1`、`server.py`、`engine.py`、`requirements.txt`、`README.md`，扫描未发现 `.venv`、模型权重、Torch/CUDA wheel、Hugging Face/ModelScope 缓存、`__pycache__` 或其它禁止 payload；短启动前未预打包 `storage`，短启动 `dist\oh-my-word-py\oh-my-word-py.exe` 5 秒成功并在 exe 同级生成 `storage\app.log` 和 `oh_my_word.sqlite3`。未执行真实安装器点击安装/卸载，也未执行真实 VoxCPM 后台安装或人工听音。
- 2026-06-14 v0.1.7 VoxCPM 安装与设置修复：安装器内嵌 VoxCPM 模型目录输入框的 `TextChanged` 处理器已移动到 `TextBox` 初始化之后，避免窗体构造时空引用；`tools/voxcpm_service/install_local.ps1` 现在会自动探测可用 Python 运行时，优先选择 3.11+，不再只依赖 `py -3.11`；设置页“检查服务”会先应用当前打开窗口里的 VoxCPM 路径和镜像设置，再查询服务状态。
- 2026-06-14 v0.1.7 验证：`py -3.11 -m pytest tests\test_controller.py tests\test_voxcpm_service_files.py tests\test_version.py tests\test_settings_window.py -q` 通过，结果为 69 个测试通过；`py -3.11 -m pytest tests -q` 通过，结果为 161 个测试通过；`git diff --check` 通过，仅有 Windows 行尾提示；`.\build\build_installer.ps1` 成功生成 `dist\oh-my-word-setup-v0.1.7.exe`，文件大小 58,186,240 字节，修改时间 2026-06-14 01:50:28；portable payload 中 `_internal\tools\voxcpm_service\` 仅包含 `install_local.ps1`、`server.py`、`engine.py`、`requirements.txt`、`README.md`，扫描未发现 `.venv`、模型权重、Torch/CUDA wheel、Hugging Face/ModelScope 缓存、`__pycache__` 或其它禁止 payload；短启动 `dist\oh-my-word-py\oh-my-word-py.exe` 约 5 秒后进程退出，`storage\app.log` 与 `oh_my_word.sqlite3` 生成完成，日志仅有 `No text-to-speech voices are available.` 警告。未执行真实安装器点击安装/卸载，也未执行真实 VoxCPM 后台安装或人工听音。
- 2026-06-17 VoxCPM Qt 异步朗读热路径重构：`app/tts.py` 已移除主线程 `urllib`/手工 `sleep()+processEvents()` 流式朗读路径，改为 Qt 原生 `QNetworkAccessManager`/`QNetworkReply` + `QIODevice` PCM 缓冲 + `QAudioSink.start(device)` pull 模式；`PronunciationService` 新增带 request tag 的异步播放信号；controller 对 `voxcpm_local` 改为在实际播放开始时记录 `last_pronounced_at`；朗读热路径不再先做同步 `/health` 探活，`VoxCpmServiceManager.start_service()` 也不再在启动前阻塞探活。
- 2026-06-17 v0.1.8 打包验证：版本号已升至 `0.1.8`，更新日志记录 Qt 异步 VoxCPM 播放链路、去除同步 `/health` 热路径探测，以及 `last_pronounced_at` 改为在实际播放开始时记录；`py -3.11 -m pytest tests\test_version.py tests\test_settings_window.py -q` 通过，结果为 6 个测试通过；`py -3.11 -m pytest tests -q` 通过，结果为 166 个测试通过；`.\build\build_installer.ps1 -SkipPortableBuild` 成功生成 `dist\oh-my-word-setup-v0.1.8.exe`，文件大小 58,191,872 字节，修改时间 2026-06-17 20:06:33；短启动 `dist\oh-my-word-py\oh-my-word-py.exe` 8 秒成功，进程名为 `oh-my-word-py`，随后已主动关闭。未执行真实安装器点击安装/卸载，也未执行真实 VoxCPM 人工听音。
- 2026-06-17 v0.1.9 VoxCPM 安装脚本回退：`tools/voxcpm_service/install_local.ps1` 现在在某个 Python 候选能输出版本但 `-m venv` 失败时，会清理残留 `.venv` 并继续尝试下一个兼容解释器；安装日志会记录每个失败候选的具体错误，便于定位目标机器的 `py -3.11`/路径组合问题。
- 2026-06-17 v0.1.9 打包验证：`app/version.py` 已升至 `0.1.9` 并补充更新日志；`py -3.11 -m pytest tests\test_version.py -q` 通过，结果为 2 个测试通过；`py -3.11 -m pytest tests -q` 通过，结果为 167 个测试通过；`.\build\build_installer.ps1` 成功生成 `dist\oh-my-word-setup-v0.1.9.exe`，文件大小 58,193,408 字节，修改时间 2026-06-17 21:17:03；portable payload 中 `_internal\tools\voxcpm_service\` 仍仅包含 `install_local.ps1`、`server.py`、`engine.py`、`requirements.txt`、`README.md`；以 `dist\oh-my-word-py` 为工作目录短启动 `oh-my-word-py.exe` 8 秒成功，随后主动停止，并在 exe 同级生成 `storage\app.log` 和 `storage\oh_my_word.sqlite3`。未执行真实安装器点击安装/卸载，也未执行真实 VoxCPM 人工听音。
- 2026-06-17 v0.1.9 安装器新报错定位：目标机器的 `install-bootstrap.log` 显示新安装器已进入 `Resolve-PythonRuntime`，但在处理 `python` / `python3` 候选时，`Get-PythonRuntimeCandidate` 的 `Arguments` 参数仍声明为 mandatory，PowerShell 会把空数组当成非法绑定并在真正探测解释器前直接抛错；这不是 Python/venv 本身失败。
- 2026-06-17 v0.1.9 安装器二次修复：`Get-PythonRuntimeCandidate` 的 `Arguments` 已改为默认空数组，允许 `python` / `python3` 这类无额外参数的候选正常探测；新增回归测试覆盖“空参数数组不应触发参数绑定错误”。`py -3.11 -m pytest tests\test_voxcpm_service_files.py -q` 通过，结果为 33 个测试通过；`py -3.11 -m pytest tests -q` 通过，结果为 168 个测试通过。此修复尚未重新打包进新的安装器资产。
- 2026-06-17 v0.1.9 本地重打包（空参数数组修复后）：在当前工作树上重新运行 `py -3.11 -m pytest tests -q` 通过，结果为 168 个测试通过；`.\build\build_installer.ps1` 成功生成新的 `dist\oh-my-word-setup-v0.1.9.exe`，文件大小 58,192,384 字节，修改时间 2026-06-17 21:36:25；portable payload 中 `_internal\tools\voxcpm_service\install_local.ps1` 已包含 `Get-PythonRuntimeCandidate` 的 `[string[]]$Arguments = @()` 修复，`_internal\tools\voxcpm_service\` 仍仅包含 `install_local.ps1`、`server.py`、`engine.py`、`requirements.txt`、`README.md`；以 `dist\oh-my-word-py` 为工作目录短启动 `oh-my-word-py.exe` 8 秒成功，随后主动停止，并在 exe 同级生成 `storage\app.log` 和 `storage\oh_my_word.sqlite3`。按用户最新要求，本轮仅本地重打包，尚未推送云端覆盖现有 `v0.1.9` Release 资产。
- 2026-06-17 v0.1.9 安装器三次修复：目标机器最新日志显示 `Selected Python runtime: py -3.11 (3.11)` 之后又打印 `Using Python runtime: py -3.11 py -3 python`，并在 `Invoke-Native` 处把数组对象误传给 `-FilePath`；根因是 `Resolve-PythonRuntime` 用 `return ,$resolvedCandidates` 返回了包裹数组，而调用端又写成 `@(Resolve-PythonRuntime)`，多套了一层数组，只有在目标机探测到多个 Python 候选时才会暴露。现已新增回归测试，禁止 `return ,$resolvedCandidates` 和 `@(Resolve-PythonRuntime)`；`tools/voxcpm_service/install_local.ps1` 已改为 `return $resolvedCandidates` 与 `$pythonRuntimes = Resolve-PythonRuntime`；`py -3.11 -m pytest tests\test_voxcpm_service_files.py -q` 通过，结果为 34 个测试通过；`py -3.11 -m pytest tests -q` 通过，结果为 169 个测试通过；`.\build\build_installer.ps1` 已再次生成新的 `dist\oh-my-word-setup-v0.1.9.exe`，文件大小 58,193,408 字节，修改时间 2026-06-17 21:51:15；portable payload 中 `_internal\tools\voxcpm_service\install_local.ps1` 已同时包含 `[string[]]$Arguments = @()` 和 `$pythonRuntimes = Resolve-PythonRuntime`，且不再包含 `return ,$resolvedCandidates` 与 `@(Resolve-PythonRuntime)`；以 `dist\oh-my-word-py` 为工作目录短启动 `oh-my-word-py.exe` 8 秒后进程仍存活，随后已主动停止，并确认生成 exe 同级 `storage\app.log` 和 `storage\oh_my_word.sqlite3`。按用户要求，本轮仍仅保留本地重打包结果，未推送云端覆盖现有 `v0.1.9` Release 资产。
- 2026-06-18 v0.1.10 VoxCPM2 运行时导入：设置页“发音”分类已新增 `导入 VoxCPM 运行时包` 主入口，`app/voxcpm_runtime.py` 负责 manifest/layout/hash/环境矩阵校验，`app/voxcpm_service.py` 支持 staging 导入、健康检查、激活与回滚；运行时状态区可显示 runtime ID、CUDA 标签、最低驱动要求和模型版本；未安装时 controller 提示已改为优先导入运行时包，`后台安装 / 更新` 仅作为兼容/兜底方案。
- 2026-06-18 v0.1.10 验证：`py -3.11 -m pytest tests/test_voxcpm_runtime.py tests/test_voxcpm_service_manager.py tests/test_settings_window.py tests/test_controller.py tests/test_voxcpm_service_files.py -q` 通过，结果为 93 个测试通过；`py -3.11 -m pytest tests -q` 通过，结果为 181 个测试通过；`.\build\build_installer.ps1` 成功生成 `dist\oh-my-word-setup-v0.1.10.exe`，文件大小 58,224,128 字节；portable payload 中 `_internal\tools\voxcpm_service\` 仍仅包含 `engine.py`、`install_local.ps1`、`README.md`、`requirements.txt`、`server.py` 五个 service-only 文件，构建前未预打包 `storage`，且未发现 `.venv` 或模型权重；以 `dist\oh-my-word-py` 为工作目录短启动 `oh-my-word-py.exe` 8 秒后进程仍存活，随后已主动停止，并确认生成 exe 同级 `storage\app.log` 和 `storage\oh_my_word.sqlite3`。本轮未执行真实安装器点击安装/卸载，也未对打包后设置窗口中的“导入 VoxCPM 运行时包”按钮做人工点击验证。
- 2026-06-18 弹窗自动朗读与 ModelScope 分包导入增强：`AppSettings` 新增 `auto_pronounce_on_popup` 和 `auto_pronounce_delay_seconds`；设置页“发音”分类新增自动朗读开关、延迟秒数输入框、`下载并导入运行时包` 按钮和 `导入模型包` 按钮；`AppController` 新增单次 `QTimer` 调度自动朗读，并在手动朗读、关闭弹窗、单词切换时取消待触发任务；`app/voxcpm_runtime.py` 与 `app/voxcpm_service.py` 已支持运行时包/模型包分离、ModelScope 下载、模型 manifest 校验，以及模型缺失时拒绝启动本地服务。
- 2026-06-18 当前工作树验证：使用 `C:\Users\Sudum\AppData\Local\Python\pythoncore-3.11-64\python.exe` 并设置 `TEMP/TMP=C:\Users\Sudum\Documents\oh my word\.tmp` 重新运行 `-m pytest tests -q`，结果为 `191 passed in 3.03s`；期间发现 `tests/test_voxcpm_service_files.py` 仍断言旧的 `cu124` 文档示例名，现已改为 `cu130` 并与稳定 spec 对齐。随后从源码启动 `main.py` 5 秒，进程存活，并确认生成 `storage\app.log` 与 `storage\oh_my_word.sqlite3`。
- 后续代码变更的默认完整验证命令仍为 `py -3.11 -m pytest tests -q`。
- 对 UI、托盘、热键或打包变更，还必须执行 Windows 运行时检查；如果没有执行，必须明确说明。

## 新 Agent 阅读路径

1. `AGENTS.md`
2. `memory/00-current-status.md`
3. `memory/01-recent-decisions.md`
4. `memory/02-risks-blockers.md`
5. `memory/03-handoff.md`
6. `README.md`
7. `docs/00-overview.md`
8. `docs/specs/00-specs-overview.md`

## 事实源

- Agent 规则：`AGENTS.md`
- 稳定文档入口：`docs/00-overview.md`
- 实现契约：`docs/specs/`
- 当前代码结构：CodeGraph 和源码文件
- 当前交接：`memory/03-handoff.md`
